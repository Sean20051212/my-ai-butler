# Web framework
fastapi
uvicorn[standard]

# LLM clients
openai
ollama

# TTS text preprocessing
opencc-python-reimplemented
pypinyin

# Vision — screenshot fallback (VLM layer, always available)
mss
Pillow

# Vision — structured perception layers (optional; chain degrades to VLM
# screenshot if these are absent).
#   playwright also needs:  playwright install chromium
#   DOM source needs the browser launched with --remote-debugging-port
playwright
pywinauto

# Environment
python-dotenv

# Long-term memory (optional — enable by setting OBSIDIAN_VAULT_PATH in .env)
# Also requires: ollama pull nomic-embed-text
chromadb

# Testing
pytest
pytest-asyncio
httpx
